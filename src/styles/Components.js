import { StyleSheet } from "react-native";

let base = {
    height: '100%'
}

export default StyleSheet.create({
    enterVillage: {
        ...base,
    },
    yourVillages: {
        ...base
    },
    openVillage: {
        ...base
    },
});