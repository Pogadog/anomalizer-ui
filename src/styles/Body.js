import { StyleSheet } from "react-native";

let base = {
    left: 20,
    right: 20,
    marginTop: 20
}

export default StyleSheet.create({
    enterVillage: {
        ...base,
    },
    yourVillages: {
        ...base
    },
});